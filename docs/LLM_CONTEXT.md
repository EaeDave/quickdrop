<!-- business-readme:context:start -->
# LLM Context

## Current business rule map

- Regra: A interface desktop (Tauri), a landing page web (`GET /`) e o cliente Windows via system tray aceitam 1 ou vários arquivos por ação via drag-and-drop, seletor ou `Ctrl+V`. A landing page também exibe o comando copiável de instalação Windows `irm https://quickdrop.eaedave.xyz/install.ps1 | iex` e mantém Linux como “em breve”. Com múltiplos arquivos, o desktop cria um ZIP temporário via Rust para caminhos locais e o cliente web/clipboard cria um ZIP via `fflate` em JS. Ambos enviam o pacote final como 1 upload para `POST /api/upload`, gerando 1 link público. Fonte: UI `src/desktop/App.tsx`; `src/desktop/tauri.ts`; comandos Tauri `src-tauri/src/lib.rs`; Backend `src/server/upload-service.ts`.
- Regra: `Ctrl+V` com imagem/arquivo do clipboard envia o arquivo colado; `Ctrl+V` com texto cria `quickdrop-paste.txt` (`text/plain`) antes do upload. No desktop Wayland, o atalho chama `wl-paste` nativo porque o WebView pode não emitir `paste` com imagem. No Windows, o app não intercepta o atalho nativamente e deixa o WebView2 entregar o `paste` padrão. Fonte: handler de teclado/paste em `src/desktop/App.tsx` e comando `uses_native_clipboard_paste` em `src-tauri/src/lib.rs`.
- Regra: upload público sem autenticação, rate limit de 20 uploads/hora/IP. Fonte: Endpoint interno `POST /api/upload` registrado em `src/server/index.ts`.
- Regra: limite padrão 500 MB e rejeição de arquivo vazio/multipart inválido. Fonte: `src/server/config.ts`, `src/server/upload-service.ts`, testes `src/server/config.test.ts`.
- Regra: URL pública `${PUBLIC_BASE_URL}/f/:shortId` redireciona para signed URL do R2 e incrementa `download_count`. Fonte: Endpoint interno `GET /f/:shortId` em `src/server/download-service.ts`.
- Regra: expiração padrão de 24h; expirados são removidos do R2 e marcados com `deleted_at`. Fonte: `src/server/expiration.ts`, Job interno `cleanupExpiredUploads` em `src/server/cleanup.ts`, CLI `src/server/cleanup-cli.ts`.
- Regra: desktop copia o link e notifica sucesso por integração nativa da plataforma. Linux/Wayland usa `wl-copy` e `notify-send`; Windows usa `tauri-plugin-clipboard-manager` e `tauri-plugin-notification`. Em múltiplos arquivos, o sucesso mostra um único link para o ZIP; se algum arquivo selecionado for inválido/vazio, o pacote não é enviado. Fonte: comandos desktop `copy_link`, `notify_success`, `upload_files` em `src-tauri/src/lib.rs` e UI `src/desktop/App.tsx`.
- Regra: no Windows, QuickDrop roda residente na system tray. Clique esquerdo ou menu `Abrir QuickDrop` abre/foca a janela; botão fechar/Esc ocultam a janela; menu `Sair` encerra o processo; o primeiro start ativa autostart automaticamente com `--tray-start` e grava marcador local para não reativar sozinho se o usuário desativar depois via menu `Iniciar com Windows`. Fonte: `src-tauri/src/lib.rs`, `src-tauri/tauri.windows.conf.json` e `src/desktop/App.tsx`.
- Regra: a instalação Windows por PowerShell (`GET /install.ps1`) baixa o instalador por `GET /windows/latest.exe`; esse endpoint usa `QUICKDROP_GITHUB_TOKEN`/`GITHUB_TOKEN` somente no backend para buscar o asset privado `QuickDrop_*_x64-setup.exe` da última release GitHub, sem expor token ao script público. `GET /` mostra o mesmo comando para cópia rápida. Depois do NSIS silencioso, o script abre o `QuickDrop.exe` instalado sem `--tray-start`, deixando a janela visível no canto direito e liberando o terminal sem esperar o processo residente da tray encerrar. Fonte: `src/desktop/App.tsx`, `scripts/install-windows.ps1`, `src/server/index.ts`, `src/server/windows-installer-service.ts`, `src/server/index.test.ts`.

## Technical map for future LLMs

 - Backend entrypoint: `src/server/index.ts` (Fastify, multipart, rate-limit, health, upload, download, cleanup startup, `GET /install.ps1` para o instalador PowerShell Windows, `GET /windows/latest.exe` para proxy server-side do asset privado no GitHub, serve a build de produção do frontend na raiz via `@fastify/static` apontando para a pasta `dist`).
- Backend CORS: `src/server/index.ts` registra `@fastify/cors` com `origin: true`, métodos `GET/POST/OPTIONS` e header `x-quickdrop-file-size`; isso permite que o WebView Tauri poste arquivos em memória ao backend remoto sem cair em erro de preflight. Regressão em `src/server/index.test.ts`.
- Persistence: PostgreSQL via Drizzle ORM on `Bun.SQL`; Drizzle Kit config in `drizzle.config.ts`; typed schema in `src/server/schema.ts`; DB helper in `src/server/db.ts`; migration SQL in `migrations/001_create_uploads.sql`.
- Storage: Cloudflare R2 via AWS SDK v3 in `src/server/r2.ts`; object keys from `src/server/ids.ts` as `uploads/YYYY/MM/{uuid}-{sanitizedFilename}`.
- Upload workflow: `src/server/upload-service.ts` accepts 1 multipart file per request, spools it to a temporary file with byte counting/limit enforcement, uploads the temp file stream to R2, then inserts metadata; short ID retry handles PostgreSQL unique violations without re-uploading R2 object.
- Download workflow: `src/server/download-service.ts` rejects missing/deleted, deletes+marks expired, otherwise signs R2 GET and redirects 302.
- Desktop Rust commands: `src-tauri/src/lib.rs` implements `upload_file`, `upload_files`, `read_clipboard_upload_inputs`, `copy_link`, `notify_success`, `dismiss_window`, `uses_native_clipboard_paste`; `upload_files` validates selected paths, creates a temporary `.zip` with unique entry names when there is more than 1 file, uploads that ZIP through the existing single-file backend endpoint, then removes generated temp files. `read_clipboard_upload_inputs` uses `wl-paste --list-types`/`--type` on Linux/Wayland to materialize clipboard image/text as a temporary local file for upload. Config reads `QUICKDROP_API_BASE_URL`; default is `https://quickdrop.eaedave.xyz` on Windows and `http://127.0.0.1:3000` on non-Windows unless the launcher/env overrides it.
- Desktop UI / Web Frontend: `src/desktop/App.tsx` funciona em ambos os ambientes (detectado via `isTauri`). No desktop usa drag-drop events do Tauri, seletor nativo e `Ctrl+V` via `wl-paste` somente quando `uses_native_clipboard_paste` retorna true; na web renderiza uma landing page com hero, card de instalação Windows copiável, link `Ver script` para `/install.ps1`, aba Linux “em breve”, upload HTML5 por drag-and-drop/input e `Ctrl+V` via Clipboard API/eventos padrão. Mostra progresso real do upload e fase `Criando ZIP...` ao compactar múltiplos arquivos. A barra superior customizada chama `getCurrentWindow().startDragging()` para mover a janela frameless no Tauri.
- Commands: `bun run server:dev`, `bun run server:start`, `bun run db:generate`, `bun run db:check`, `bun run db:migrate`, `bun run cleanup:run`, `bun run desktop:dev`, `bun run desktop:build:web`, `bun run desktop:build`, `bun run desktop:build:windows`, `bun run desktop:install`, `bun run desktop:install:local`, `bun run waybar:install`, `bun run local:server:up`, `bun run local:server:stop`, `bun run quickdrop:install`, `bun run quickdrop:install:local`, `bun run typecheck`, `bun test`.
- Waybar install: `scripts/install-desktop.ts` copies `quickdrop` and `quickdrop-waybar`; `scripts/install-waybar-module.ts` idempotently patches `~/.config/waybar/config.jsonc` with `QUICKDROP_API_BASE_URL=https://quickdrop.eaedave.xyz` by default, backs it up, and restarts Waybar via `omarchy restart waybar` when available. Override with `QUICKDROP_API_BASE_URL=... bun run waybar:install`; `quickdrop:install` builds+installs the desktop against the remote backend for reboot-safe daily use.
- Windows tray: `src-tauri/Cargo.toml` enables Tauri `tray-icon`/`image-png` and native plugins for clipboard, notification, and autostart. `src-tauri/src/lib.rs` builds the tray only on Windows, uses `icons/icon.png`, positions the default/opened window above the tray/work-area corner instead of at the cursor, hides instead of closes on `dismiss_window`, enables autostart once on first launch, persists `autostart-configured` under the app config dir, and autostarts with `--tray-start`. `scripts/install-windows.ps1` intentionally does not pass `--tray-start` after installation: it waits for NSIS only, then starts the installed `QuickDrop.exe` detached/visible so the PowerShell prompt returns immediately. `src-tauri/tauri.windows.conf.json` enables NSIS bundling and Windows icons.
- Deploy backend: `Dockerfile` utiliza build multi-estágio para compilar o frontend (`dist/`) com dependências de desenvolvimento e copiá-lo para a imagem final junto ao backend e `scripts/install-windows.ps1`. O entrypoint `scripts/docker-entrypoint.sh` roda as migrações e inicia o Fastify na porta `3000`. Coolify reconstrói a imagem automaticamente após o push. Para o instalador Windows funcionar com repo privado, o ambiente do servidor precisa de `QUICKDROP_GITHUB_TOKEN` (preferido) ou `GITHUB_TOKEN`; `QUICKDROP_GITHUB_REPOSITORY` defaults to `EaeDave/quickdrop`.
- Local Docker backend: `compose.yml` has an optional `server` service behind profile `local-server`, with Postgres healthcheck, `restart: unless-stopped`, port `127.0.0.1:3000`, and `DATABASE_URL` overridden to `db:5432`. `quickdrop:install:local` starts it and installs Waybar pointed to `http://127.0.0.1:3000`.

## Conflicts and unknowns

- End-to-end upload/download requires real Cloudflare R2 credentials and PostgreSQL running; repository defaults do not contain secrets.
- Desktop Wayland flow requires local `wl-copy`, `wl-paste`, `notify-send`, Tauri Linux system dependencies, and a graphical Wayland session. Windows tray/notification verification requires a Windows machine with WebView2; notification branding is only correct for installed apps.
- Deploy Dockerfile covers the backend HTTP service only. Daily desktop use should prefer the remote backend unless the user explicitly wants the optional local Docker backend; the Windows executable defaults to `https://quickdrop.eaedave.xyz`, and the Waybar launcher injects the same remote URL by default. The public Windows installer route requires a server-side GitHub token at runtime; a token present only in GitHub Actions is not enough for Coolify/runtime.

## History

- 2026-06-22: Implementado MVP descrito em `docs/mvp.md` com base nas convenções de `docs/CLAUDE.md`; fontes inspecionadas/implementadas incluem `package.json`, `tsconfig.json`, `drizzle.config.ts`, `src/server/*`, `src-tauri/*`, `src/desktop/*`, `migrations/001_create_uploads.sql`, `.env.example`, `compose.yml` e README. O usuário esclareceu que o projeto deve usar Drizzle como ORM, então a persistência usa Drizzle ORM sobre `Bun.SQL`.
- 2026-06-23: Adicionado instalador automático do módulo Waybar; fontes atualizadas incluem `scripts/install-desktop.ts`, `scripts/install-waybar-module.ts`, `scripts/quickdrop-waybar`, `README.md` e `package.json`. O snippet manual aponta para `~/.local/bin/quickdrop-waybar` com `QUICKDROP_API_BASE_URL=https://quickdrop.eaedave.xyz`, não para o binário direto.
- 2026-06-23: Adicionado deploy Docker/Coolify para backend: `Dockerfile`, `.dockerignore`, `scripts/docker-entrypoint.sh`, `.env.example`, README e LLM context. Container roda migrações por padrão via `RUN_MIGRATIONS_ON_START=true` antes de iniciar o Fastify.
- 2026-06-23: Adicionado seletor local de arquivos pela seta na UI desktop, mantendo drag-and-drop; fontes atualizadas incluem `src/desktop/App.tsx`, `src/desktop/tauri.ts`, `src/desktop/input.css`, `src-tauri/src/lib.rs`, `src-tauri/capabilities/default.json`, `package.json`, `src-tauri/Cargo.toml`, README e LLM context.
- 2026-06-23: Documentado e automatizado caminho persistente pós-reboot: Waybar/desktop apontam para o backend remoto por padrão via `quickdrop:install`, e o Compose ganhou backend local opcional com profile `local-server` e `restart: unless-stopped` para fallback/desenvolvimento local.
- 2026-06-23: QuickDrop desktop passou a aceitar múltiplos arquivos no drag-and-drop e no seletor local gerando um único link: `src-tauri/src/lib.rs` cria ZIP temporário local para seleções com 2+ arquivos e envia ao backend como 1 arquivo; fontes atualizadas incluem `src/desktop/App.tsx`, `src/desktop/tauri.ts`, `src/desktop/input.css`, `src-tauri/src/lib.rs`, `src-tauri/Cargo.toml`, `src-tauri/Cargo.lock`, `scripts/install-waybar-module.ts`, `scripts/install-waybar-module.test.ts`, README e LLM context.
- 2026-06-23: Implementada interface Web do QuickDrop sob `GET /`: Fastify registra `@fastify/static` servindo de `dist/`; `src/desktop/App.tsx` e `src/desktop/tauri.ts` foram reescritos com detecção de ambiente e fallbacks apropriados (HTML5 drag-drop, input do tipo file, `fflate` para compactar múltiplos arquivos em ZIP no lado do cliente web, progresso real de upload via `XMLHttpRequest` e Clipboard API).
- 2026-06-23: Corrigido upload de clipboard no Waybar/Tauri contra backend remoto: o módulo instalado continuava apontando para `https://quickdrop.eaedave.xyz`, mas o WebView fazia `OPTIONS /api/upload` cross-origin e o backend não respondia CORS. Adicionado `@fastify/cors`, teste `src/server/index.test.ts` para o preflight com `x-quickdrop-file-size`, e documentação do fluxo `Ctrl+V` (imagem/arquivo/texto).
- 2026-06-23: Corrigido `Ctrl+V` de imagem no Waybar/Tauri: WebKitGTK nem sempre entrega imagens pelo evento web `paste`, então `Ctrl+V` no desktop agora intercepta `keydown`, lê tipos/dados via `wl-paste`, grava um arquivo temporário `quickdrop-clipboard-*`, faz upload pelo caminho Rust existente e remove o temporário depois.
- 2026-06-23: Adicionado cliente Windows com system tray no Tauri: feature `tray-icon`, plugins nativos de clipboard/notification/autostart, `dismiss_window` que oculta no Windows, menu `Abrir QuickDrop`/`Iniciar com Windows`/`Sair`, config `tauri.windows.conf.json`, script `desktop:build:windows` e ícones reais em `src-tauri/icons/`.
- 2026-06-23: Ajustado padrão do executável Windows: `DesktopConfig::from_env` usa `https://quickdrop.eaedave.xyz` quando `QUICKDROP_API_BASE_URL` não está definido no Windows, mantendo override por env para testes locais; README e LLM context atualizados.
- 2026-06-23: Ajustado comportamento da janela Windows: QuickDrop não abre mais na posição do mouse por padrão; o posicionamento usa a área de trabalho/tray no canto inferior direito (ou o retângulo do clique da tray quando disponível) e a barra superior customizada agora arrasta a janela via `startDragging`; fontes atualizadas incluem `src-tauri/src/lib.rs`, `src/desktop/App.tsx`, `src/desktop/input.css`, `src-tauri/capabilities/default.json`, README e LLM context.
- 2026-06-23: Windows agora ativa autostart automaticamente no primeiro start e grava marcador local para respeitar desativação manual futura; também foi adicionado `scripts/install-windows.ps1`, servido por `GET /install.ps1`, que baixa o asset `QuickDrop_*_x64-setup.exe` do último GitHub Release (ou `QUICKDROP_WINDOWS_INSTALLER_URL`) e roda o NSIS silencioso com `--tray-start`. Fontes atualizadas incluem `src-tauri/src/lib.rs`, `src/server/index.ts`, `src/server/index.test.ts`, `Dockerfile`, `scripts/install-windows.ps1`, README e LLM context.
- 2026-06-23: Alterado instalador Windows para funcionar com repositório GitHub privado sem Actions: `scripts/install-windows.ps1` baixa de `GET /windows/latest.exe`, e o backend usa `src/server/windows-installer-service.ts` com `QUICKDROP_GITHUB_TOKEN`/`GITHUB_TOKEN` server-side para resolver `/releases/latest` e baixar o asset via API GitHub. Testes cobrem script, proxy feliz e falha sem token; README documenta env de deploy.
- 2026-06-23: Corrigido o script público Windows para não ficar preso enquanto o app residente da tray continua rodando: removido `/R /ARGS --tray-start` do NSIS, o script agora espera apenas o instalador, localiza o `QuickDrop.exe` instalado via registro/fallbacks e inicia o app visível sem argumentos. Regressão adicionada em `src/server/index.test.ts`; README atualizado com o comportamento de janela aberta no canto direito.
- 2026-06-23: Página principal web ganhou landing page estilo Bun com comando copiável `irm https://quickdrop.eaedave.xyz/install.ps1 | iex`, link para visualizar o script, aba Windows ativa e Linux “em breve”, mantendo a área de upload web abaixo/do lado. Fontes alteradas: `src/desktop/App.tsx`, `src/desktop/input.css`, README e LLM context.
<!-- business-readme:context:end -->
